{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build":"turbo run build"
  },
  "devDependencies": {
    "cz-emoji": "^1.3.2-canary.2",
    "turbo": "^1.6.3"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-emoji"
    }
  }
}
